import styled from "styled-components";

export const ExitButton = styled.img`
  position: absolute;
  right: 1rem;
  bottom: 1rem;
  width: 4rem;
  height: 4rem;
  opacity: 0.5;
  cursor: pointer;
`;
